# Calysto Dungeon Director V6 evidence — 2026-09-04

Overall status: **PENDING**.

This receipt records the tooling/bootstrap phase only. No Content asset was
created, saved, renamed, or deleted during this phase, and no retirement tool
was executed.

## Implemented tooling

- `Create-CalystoDungeonDirectorPolicyV6.py`: create-once/read-only validation,
  exact singleton path/class, native defaults, four canonical hashes, and cook
  bundle diagnostics.
- `Create-CalystoDungeonDirectorRuntimeAssetsV6.py`: safe duplication to the
  three definitive unversioned Blueprint paths; blocked until the unversioned
  native chest parent exists.
- `Create-CalystoDungeonDirectorV6DecalMaterials.py`: project-owned decal
  material graph and three surface instances, using only byte-pinned
  `T_Splat_04` and `T_Splat_N_04`.
- `Audit-CalystoDungeonDirectorLegacyReferencesV6.py`: read-only transitional or
  final Asset Registry/text audit.
- `Retire-CalystoDungeonDirectorLegacyAssetsV6.py` and wrapper: audit-only by
  default; explicit deletion requires a complete acceptance receipt and exact
  V6 gameplay hash.
- `Run-CalystoDungeonDirectorV6NativeAutomation.ps1`: exact-count,
  warning-free native gate scaffold with a zero-Content-mutation assertion.

## Static verification

- Python `py_compile`: **PASS** for all five V6 Python tools.
- PowerShell parser: **PASS** for both V6 PowerShell wrappers.
- Unreal execution: **PENDING**.
- Native runtime/editor build: **PENDING**.
- V6 asset creation and second read-only pass: **PENDING**.
- Blueprint compile, PIE, statistics, visual QA, performance, cook, package,
  smoke, determinism, retirement, and generated-output purge: **PENDING**.

## Pinned source textures

- `T_Splat_04.uasset`:
  `33B6BD0949AA4F7EC0A6D0FB87307BE1924967A4AE019287F82806649B35DB3D`
- `T_Splat_N_04.uasset`:
  `0DE564AFB5AEEF377A7DB46CCC903B4AB144BC1A77E5DF01EC5798C6E853DB74`

The V6 decal tool treats both packages as read-only and rejects any additional
`/Game/RealisticBlood` dependency.

## Required next evidence

Record the exact commit/snapshot, build logs, creator receipts, V6 hashes,
protected hashes, native test count, deterministic statistical report, PIE and
visual captures, preload/hitch timings, cook manifests, packaged smoke results,
legacy audit, retirement receipt, and reclaimed generated bytes. Until those
artifacts exist, V6 cutover and legacy removal are not PASS.
