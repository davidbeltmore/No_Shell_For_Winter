# Calysto Director V7 migration gaps - 2026-09-08

Status: **PENDING full migration/runtime acceptance.** G2/G4/G5/G6/G7 mapped values have a clean transient Editor roundtrip; that receipt's complete migration remains false. G1 typed traits/consumers are implemented with pure and focused native PASS; their current-schema Editor verification and full snapshot integration remain PENDING. Parent owns G3, builds, Editor, PIE and assets. Source game `D:/Projects UE5/LustAsDeadlySin` remains read-only; migration input is the target-owned immutable V6 asset.

## Exact evidence and scope

- Historical staged receipt inspected at audit: [`ImportDirectorStaged.json`](../../../Saved/Migration/CalystoDungeonDirectorV7/ImportDirectorStaged.json), generated `2026-09-05T12:28:28.009215+00:00`, `STAGED_AUTHORING_VERIFIED`, 0.422 seconds, zero saves/dirty packages, mapped-value roundtrip true, complete migration false, **68 PENDING leaves**. Its zero-save staging result does not prove a second saved import is idempotent. Subsequent operator runs archive receipts in fresh named evidence directories; assess their exact source revision separately.
- Receipt SHA-256: `3ec72a8f474d6c91ed6e4f0abbd16cf5263c777d48bf3cd1481688ef9405dd94`.
- Complete source: `Saved/Migration/CalystoDungeonDirectorV7/SourceV6_Complete_F08324D234872CC6BDD7A12F3D5DBEEE3BF03502CBA5FA32187CFBF7AB029323.json`, SHA-256 `e3fa775802d64012bf731a5ad090898d8dfc97e86e4aa351da7931d863a9e21f`.
- Exact matching candidate export: `CandidateV7_ED7C013BED8F094DF080342E7D722C505583154D8A9F36C4BB27413940C65953_7E39035BFFF7.json` in that directory, SHA-256 `73e69e2db4e687e3becef7351d4c34ff2d05196c9cf821fd90dcbb63a1edbe14`. The older `_27FDFDE9874F` export is not the matching latest receipt.
- Current target input `Content/_Game/Data/CalystoDungeon/V6/DA_CalystoDungeonDirectorPolicy.uasset` re-hashes to `f08324d234872cc6bdd7a12f3d5dbeee3bf03502cba5fa32187cfbf7ab029323`. `Content/_Game/Data/CalystoDungeon/DA_CalystoDungeonDirector.uasset` is absent at this audit; no live loaded-state claim follows.
- `ImportDirector.json` is the earlier `SelectionWeight` failure, not a later saved import. Staging predates the current lighting schema. Current-source staging, saved import, second-import zero saves, and complete runtime equivalence all remain PENDING.
- The parent-provided consolidated build result and `Native_Consolidation_20260908/Report/index.json` do not close migration: the 77-test report has no V7 Lighting/Flicker test entry. Saved-only lighting draft tests are not registered production tests. Actual process exits, source hashes and live/package evidence remain separate acceptance requirements.

## G2/G6 implementation follow-up

`python Tools/Migration/Test-CalystoDirectorImporter58.py -v` passed **6 pure tests in 0.147 seconds, process exit 0**. Tests check all four exact mode mappings, scalar-zero/saturation behavior, exact-field rejection, size-set order preservation and invalid sets, and the three actual archived Styles against the previous candidate export. Every nonlighting Style value, native torch entry and shared light placement remains equal to that archive. The test loads only selected pure definitions through AST; it never imports Unreal or runs importer main.

That G2/G6 mapper revision covers **16 of the original 68 leaf paths in pure conversion** (3 Lighting.Mode + 13 sizes); its remaining count was 52. This is not an updated Editor receipt: the immutable last staged report still has 68 PENDING, and current-schema roundtrip, actual saved import/idempotence, runtime behavior, visual QA and packages remain PENDING. The historical 77-test observation above describes that exact report; later parent-owned production lighting tests require their own receipts. No acceptance criterion is promoted by these six helper tests.

## G4 metadata implementation follow-up

`FEFCalystoTheme` now retains Description and PreviewColor under `WITH_EDITORONLY_DATA`. Native Advanced contains multiline author notes with an explicit no-gameplay-effect tooltip; PreviewColor has neither Edit nor BlueprintVisible flags, so it cannot masquerade as a material control. The existing editor-only `UEFCalystoDirectorEditorLibrary::ExportAuthoredField(RoomThemes)` exposes both on demand. No custom Details banner, new inspector framework, material consumer or runtime content bridge was added. Source V6 uses these values in its authoring identity hash only; its existing `Policy` test already distinguishes authoring text from gameplay identity.

`Mapper.theme_metadata`, called by `Mapper.theme`, retains exact source text and every RGBA channel without color conversion/clamping, and records the ten leaf dispositions as CORRECTED to editor-only metadata. Missing fields/nonfinite or malformed channels reject with the exact source path. Tests compare both actual archived Themes against the prior candidate: every nonmetadata value is unchanged, the two descriptions/eight channels match independent expected values, and escaped notes/HDR/alpha survive pure export parsing.

`python Tools/Migration/Test-CalystoDirectorImporter58.py -v`: **9 tests PASS, 0.162 seconds, process exit 0**; no Unreal import or asset operations. Existing native `Director.Editor.CompleteFieldExport` now checks metadata text/color roundtrip, duplication and property flags; `Director.Authoring.CompilationAndPrecedence` now checks that changing metadata leaves resolved materials and visual dependencies unchanged. Their new assertions, native Undo/Redo, cooked stripping and live Details/inspector verification remain **PENDING**, not proved by Python tests. The transaction's authored-configuration receipt hash may change with author notes; it is not a random-domain input.

Together G2/G4/G6 now cover **26 original leaf paths in pure conversion; 42 remain unresolved**. The saved staged receipt still has its original **68 PENDING** until a current-schema Editor migration is run. The original acceptance ledger remains 31 criteria with no promotion from this bounded subtask.

## G7 safety-ceiling implementation follow-up

`FEFCalystoSafetyCeilings`, under `FEFCalystoDirectorAdvanced.HardCeilings`, retains the six nonnegative integer capacities with defaults 25/8/3/4/4/36. `UEFCalystoDungeonDirectorAsset::Compile` rejects negative globals at exact `Advanced.HardCeilings.<field>` paths and enabled positive-weight Style capacities outside `[0, ceiling]` at exact `Styles[i].FloorBudgets.<field>` paths. It never edits/clamps authored values. The existing immutable compiler copy freezes both ceilings and Style budgets. Disabled/zero-weight Styles retain their authored data but must pass validation before activation.

The actual consumer is the existing `FEFCalystoContentReservationPlanner`: `Validate` rejects excess initial usage; `CapacityUpperBound`/`Fits` reserve against the shared selected Style bucket and total capacities before Chance. The compile-time inequality proves these effective capacities also fit the global ceilings, including Theme Replace/Extend. A new `ReservationStyleInactive` guard prevents direct planner requests from selecting an inactive Style whose gameplay fields were intentionally not compiled. No second budget owner, probability adjustment, post-draw truncation or sampler was introduced. Full-floor surface/materializer wiring is still absent and PENDING; no actual population claim follows.

`Mapper.safety_ceilings` maps all six exact source leaves to the matching fields, preserves integer values without defaults/clamping, and rejects missing, fractional, nonfinite, malformed or out-of-int32 values with exact source paths. `python Tools/Migration/Test-CalystoDirectorImporter58.py -v`: **12 tests PASS, 0.202 seconds, process exit 0**, including the actual archived ceiling values. Prepared native tests `Director.Authoring.SafetyCeilings` and `Director.Content.SafetyCeilingsAndSharedUsage` cover all six limits, zero/negative boundaries, exact errors, immutable copies, inactive activation, three-room shared capacity, existing usage, Theme Replace/Extend, feasible Amount mass 1/3 before unchanged Chance, unchanged manifest identity when only an unused ceiling changes, and atomic rejection. Existing `Director.Content.ExistingUsageAndInputBounds` now checks direct inactive/zero-weight requests. These new native assertions remain **PENDING execution**.

G2/G4/G6/G7 together cover **32 of the original 68 leaf paths in pure conversion; 36 remain unresolved** (G1=25, G3=6, G5=5). The historical staged receipt still has 68 PENDING. Current-schema Editor staging/saved import/idempotence, native current-source checks and real content realization remain distinct gates; the original acceptance denominator is unchanged.

## Subsequent native receipts (parent-owned runs)

- G4: `Native_Native_ThemeMetadata_20260908/StrictSummary.json` reports **2 expected/actual native tests PASS**, no failures, launcher exit **0**, 47.137901 seconds. `Report/index.json` SHA-256 `e9b93015dce0a55f54ee524b23660e126ed941d4160cb39c37bb769995c7a516` proves the extended `CompleteFieldExport` and `CompilationAndPrecedence` assertions. This supersedes their prepared/PENDING notes above only; actual Editor Undo/Redo, loaded Details and current-schema migration are still PENDING.
- G7: `Native_SafetyAndLitFixture_20260908/StrictSummary.json` reports **4 expected/actual native tests PASS**, no failures, launcher exit **0**, 47.362862 seconds: the three G7 checks named above plus `Native.TransientParityFixture`. `Report/index.json` SHA-256 `d7f5d09be9d40f9dab557ad35404286f7f61574dc2a46e7e01b5de274b52c12a`; strict receipt SHA-256 `55cbd16e44e9b0859fc4df6ac1ca09603184b4fc24d639fbad27d08e86a69b4d`. This supersedes G7 native execution PENDING only. Real population, current-schema Editor import and full acceptance remain separate. All paths in this section are under `Saved/Migration/CalystoDungeonDirectorV7/`.

## G5 explicit fade correction and pooled realization

`Mapper.decals` now retains all five exact source `FadeStartDistanceCm=2500` values as hidden editor-only `FEFCalystoDecals.SourceFadeStartDistanceCm`, recording `MATHEMATICAL_CORRECTION` / `RETIRED_DISTANCE_APPROXIMATION` with the exact old formula. Authored `FadeScreenSize=.01` preserves the old pool's native default; `CullDistanceCm=4000` is now an actual camera-distance visibility threshold. The old size-dependent approximation is explicitly retired, not described as equivalent world-distance fading. This satisfies the master contract's accurately named screen fade plus real cull option; it does not require an unimplemented DecalColor material path.

The unversioned `AEFCalystoDecalPoolOwner` implements hidden `Begin`/`Verify`, explicit `ActivateCommitted`, camera-distance culling and idempotent `Release`. It consumes already selected typed proposals, checks actual native room/opportunity ownership and physical component/instance support, enforces authored shared capacities and retains explicit material/texture leases. It never resamples or starts native generation. Pool source compiled in the parent-owned build; selected-decals capability preflight remains blocked until actual surface-planner, material closure and transaction wiring are verified. Full integration, rendering and packaged evidence remain PENDING. Exact API, scope, fingerprints and initial failed native receipt are in [the decal pool evidence](Calysto_Director_V7_Decal_Pool_20260908.md).

`python Tools/Migration/Test-CalystoDirectorImporter58.py -v`: **13 pure tests PASS, 0.251 seconds, exit 0**. The added archive test proves all five exact legacy values, unchanged 4000 cm cull, explicit .01 threshold, unique correction rows and unchanged source input. Existing native `CompleteFieldExport` now also checks hidden metadata roundtrip and property flags; these added G5 assertions await the next schema build/run. G2/G4/G5/G6/G7 cover **37 of the original 68 leaves; 31 remain unresolved (G1=25, G3=6)**. This is source-mapping progress; the historical Editor receipt and the separate original **31-criterion acceptance denominator** are unchanged.

Subsequent `Native_DecalSchema_20260908/StrictSummary.json`: **3 expected/actual tests PASS**, 48.249075 seconds, launcher exit 0, no failures. This executes both corrected pool tests and the new `CompleteFieldExport` metadata assertions, superseding their PENDING notes above. Strict SHA-256 `23cc22b32f9c9496aacc2b24b2225fe68555945e7442ffdc08da823155f6891f`; report SHA-256 `4247fe23fcb7ac77deedf6ec77131881996a48691ccfbd1c82dd20db1cd7d616`. Full adapter/planner/material/transaction integration and current-schema Editor migration remain PENDING.

## Exact partition of the 68 leaves

Later parent-owned `AuthoringStage_20260908_G24567/ImportDirectorStaged.json` (`2026-09-08T12:48:56.252392+00:00`) is `STAGED_AUTHORING_VERIFIED`: .39 seconds, 2912 records, mapped roundtrip true, no authoring errors/saves/dirty packages, source unchanged; **31 leaves remain (G1/G3), complete migration false**. SHA-256 `e8215305d657c7130ba009ad71c698411c039ae61d72f2ecf59c556825082144`. This is the exact pre-G1/G3 stage, not a saved master or full runtime equivalence.

G1 follow-up now retains all 25 normalized leaves and adds explicit bounded typed Chance/exact-entry Weight bindings consumed before planner feasibility. All 15 pure importer checks PASS (.357 seconds, exit 0); new native assertions/build and current-schema stage remain PENDING. Initial bindings stay empty and adaptation disabled. [Trait binding evidence](Calysto_Director_V7_Trait_Bindings_20260908.md) records formulas, cache/work bounds, source hashes and missing snapshot/UI integration. Parent's concurrent G3 changes have separate ownership/evidence; this report does not infer their native completion from mapper counts.

Subsequent parent-owned `Native_TraitsArchitectureCopy_20260908/StrictSummary.json`: **6 expected/actual tests PASS**, no failures, 47.760798 seconds, launcher exit 0, including both G1 tests and amended capacity test. Strict SHA-256 `b84b67a6f1af39a60277ddcaf22086c1267315d6dda408d9eb5d0f2d7e35f7bd`; report `ba975919bde4ee8f629176c86126484566ca02026257f0174ee9a7f66da73b25`. This supersedes G1 native PENDING notes only; exact current-schema stage and real integration remain separate.

`i=0..2` means Standard, Compact, Branching; `j=0..1` means Forge, Shrine. Every pending source leaf belongs to exactly one row; these groups do not change the 31-criterion acceptance denominator.

| Group | Exact source field family | Leaves | Observed source values | Current disposition |
| --- | --- | ---: | --- | --- |
| G1 | `Styles[i].Traits.{Mystery,Danger,Safe,Abundance,ClothingInfluence}` and `RoomThemes[j].Traits.{Mystery,Danger,Safe,Abundance,ClothingInfluence}` | 25 | Branching Mystery=.30; Forge Danger=.85, Abundance=.45; Shrine Mystery=.65, Safe=.70; other values zero | Typed schema/compiler/probability/planner and mapper implemented; pure/focused native PASS. Current-schema import, entry-name UI and full snapshot wiring PENDING. |
| G2 | `Styles[i].Lighting.Mode` | 3 | Warm, Balanced, Balanced | Importer mode/distribution mapping implemented and pure-tested; current-schema Editor staging and actual light/PIE verification remain PENDING. |
| G3 | `Styles[i].Decoration.{Density,MaximumDecorationsPerRoom}` | 6 | Density=.35; per-room cap=4 for all Styles | V6 values hash-only in inspected runtime; V7 native optional-decoration execution/cap absent and rejected by capability preflight. |
| G4 | `RoomThemes[j].Description` and `RoomThemes[j].PreviewColor.{R,G,B,A}` | 10 | Exact prose in source; Forge=(1,.32,.02,1), Shrine=(.03,.32,1,1) | Editor-only destination/import mapping and focused native roundtrip PASS; loaded Details/Undo and actual Editor migration PENDING. Preview is not a material assignment. |
| G5 | `Styles[i].Decals.FadeStartDistanceCm` and `RoomThemes[j].Decals.FadeStartDistanceCm` | 5 | 2500 cm for all profiles | Exact hidden metadata retained; old approximation explicitly retired with formula. Actual pooled hard cull/named screen fade implemented. Pure and corrected native pool/export tests PASS; Editor migration/full integration PENDING. |
| G6 | `PerformanceAndSafety.ValidatedDungeonSizes[0..12]` | 13 | Exact contiguous set 18..30 | Importer now verifies exact unique 18..30 correspondence and records every source leaf; invalid/sparse sets remain PENDING and block staging. Editor verification remains PENDING. |
| G7 | `PerformanceAndSafety.HardCeilings.{MaximumEnemies,MaximumLooseFood,MaximumChests,MaximumLootActors,MaximumSpecialEvents,MaximumDirectorActors}` | 6 | 25, 8, 3, 4, 4, 36 | Six-field compiler guard/importer implemented; existing feasibility consumes compile-validated Style limits. Pure and focused native assertions PASS; Editor migration and actual floor population PENDING. |

## Concrete next edits and owners

**G1 - explicit typed behavior implemented; integration pending.** `FEFCalystoTraits` retains all five fields per Style/Theme; `FEFCalystoTraitBinding` names Style/Theme/Snapshot input, trait, typed content role and Chance or exact-entry Weight. The compiler validates exact fields/targets. `ResolveTraitMultiplier` applies authored linear endpoint effects and one aggregate bound; the planner caches effective values before feasibility/selection without changing frozen payloads or RNG identity. `Mapper.traits` preserves all 25 original values; empty initial bindings avoid invented gameplay associations. Native tests exercise every trait/source and exact neutral-off behavior. Next run those tests, verify current-schema import and native entry-name authoring UI, then implement actual immutable snapshot capture before full enabled behavior.

The former scalar-only planner input is replaced by optional typed `TraitSnapshot`; the existing bounded scalar primitive supplies the final aggregate clamp. `ValidateCapabilities` still rejects enabled adaptation. Actual player/clothing/ecology collection and outcome history are not inferred from normalized authored traits or pure planner tests.

**G2 - importer correction implemented; Editor/runtime evidence PENDING.** `Mapper.lighting`, called by `Mapper.style`, now replaces Fixed intensity with explicit source-semantic distributions and records both source Mode and IntensityMultiplier as CORRECTED. `EFCalystoDungeonRuntimeV6.cpp:867` supplies the actual old semantics. Recorded notes identify the restored runtime jitter and separate saturation ceiling; height/spacing/intensity output uses the current production `FEFCalystoLighting` properties. Unknown modes, malformed/nonfinite multipliers and values outside 0..4 fail with exact source-field errors.

| Imported source mode | `Lighting.WallLightHeight` uniform, cm | `Lighting.WallLightTileDistance` weighted outcomes |
| --- | --- | --- |
| Warm | [170,210] | 8:1, 9:2, 10:1 |
| Balanced | [180,220] | 9:1, 10:2, 11:1 |
| Cold | [200,240] | 10:1, 11:2, 12:1 |
| Dark | [220,260] | 12:1, 13:2, 14:1 |

The importer maps scalar `m` to uniform intensity `[.92*m,1.08*m]` with explicit `IntensityCeiling=4`, preserving saturation mass rather than clipping the input range. Current archived multipliers are all 1; the pure test at m=4 verifies that half the uniform mass remains above the ceiling before saturation. Unknown enum values reject before native import. Old mode names did not change color/temperature; keep enum-to-values translation in the importer only. Disabled flicker remains the initial setting; no source-authored flicker setting exists to claim preserved.

Current source path is `FEFCalystoLightingResolver::Resolve` -> `FEFCalystoNativeAdapter::Prepare` -> `ObserveLighting`/`VerifyLighting` -> `UEFCalystoFloorLightingComponent::Initialize`, `ApplyAtElapsed`, `Verify`, `Release`. It writes native height/tile spacing, freezes the Lighting random domain with AttemptIndex=0, binds owned native torch ActorReference outputs, applies baseline intensity and restores it during cleanup. This is **implemented but unverified**, not hash-only. Current preflight supports only one enabled native torch alternative, Wall Middle, and the exact native particle-effect contract.

`LightingImplementation_20260905/NativeTorchRead.json` records a Movable/Unitless point light with baseline 10000, empty exported construction/event bodies and preserved `FXS_LowPolyTorch`; its Niagara OverrideParameters read failed. Do not imply particle brightness/emissive/flicker equivalence from point-light intensity. Register focused production resolver/component tests (CDF boundaries, 25/50/25 spacing, saturation, independent identity, baseline restoration, unsupported components), then inspect actual selected torch transforms/intensities/effects during bounded real PIE. A future successful test does not retroactively validate this staged export.

**G3 - implement the promised optional-decoration function.** `FEFCalystoSurfaceDecoration` has Chance and Alternatives, but no combined per-room decoration budget; `Mapper.decoration` currently emits 100% Chance. Add an explicit Style-wide optional-decoration room limit and reserve its finite native opportunities across zones. Bind existing per-opportunity Chance to an explicit 35% initial setting and map cap 4 without treating it as an Amount or separate four-per-zone allowance. Document this as implementing previously unused density/cap controls; do not call the old behavior equivalent. Owner: native opportunity/reservation integration in `FEFCalystoNativeAdapter`, using the shared probability library and exact realization/rollback. Required structural slots and progression routes stay unaffected. Preserve all original catalogs, mesh/actor/baked-PCG/Empty payloads and transform data; retire the misleading unused Density control only after its explicit replacement is mapped and tested.

**G4 - implemented authoring retention; focused native verification PASS.** The editor-only fields, Advanced notes, hidden swatch, existing on-demand complete export and ten-leaf mapping are implemented as described above. Material selection still uses Theme surface overrides and actual material assets; preview metadata is not a random-domain input. Next verify native Undo/Redo and clean loaded Details, then stage through the current-schema importer. Use the exact native receipt above without extending it to unrun gates.

**G5 - implemented explicit correction; integration PENDING.** `UEFCalystoDecalPoolComponentV6::AcquireDecal` derives a screen threshold using `radius=max(Size.Y,Size.Z)`, `clamp(lerp(radius/CullDistanceCm,radius/FadeStartDistanceCm,.5),.0001,.25)`, then takes its maximum with the pool screen threshold. The observed block is `EFCalystoDecalPoolV6.cpp:531`. This was not exact world-distance fade/culling.

The approved implementation retains 2500 as hidden source metadata and the explicit old formula in mapping evidence, retires the misleading approximation, and uses native screen threshold .01 with independent real hard culling. No legacy runtime mode, MID or actor per stain is introduced. Next run the corrected focused pool fixture and G5 native export assertions, then verify the actual shared material closure and reserve real surfaces before transaction wiring. Actual camera/FOV rendering, all selected realizations and rollback remain integration gates; capability guards stay intact.

**G6 - exact-set correspondence implemented; Editor verification PENDING.** `Mapper.validated_sizes` accepts exactly 13 finite integral unique values equal to the set 18..30 in any order, preserves original indexed values, and records each leaf as CORRECTED against `Styles[*].Layout.DungeonSize` and its existing native compiler guard. Main records `validated_layout_sizes_equivalent`; any different, duplicate, fractional, malformed or sparse set keeps PENDING mappings and raises before creating a transient/native candidate. Empty/invalid containers retain a parent-field PENDING record. The original list remains in the immutable source export. No min/max coercion, resampling, empirical certification or new runtime allowlist is claimed; the finite size/depth acceptance matrix remains unchanged.

**G7 - independent guard implemented; focused native verification PASS.** The schema, exact compiler errors, immutable copy, direct-inactive-request rejection, six-leaf importer mapping and focused assertions above are implemented and have the named native receipt. Active Style budgets remain the effective planner capacities; no hidden clamping or change to probability domains occurs. Next run current-schema Editor staging/roundtrip. Real materializer/world bridge validation remains separate.

## Dependencies that the 68-leaf count does not measure

`MAPPED` means native data retention only. `FEFCalystoNativeAdapter::ValidateCapabilities` currently rejects nonempty Style/Theme Content, Style Decoration, Theme Architecture, enabled adaptation and selected decals; it also restricts architecture alternatives/room bounds. Fixing the 68 mapper rows alone cannot produce a playable full candidate. Preserve these capability rejections until corresponding world reservation/materialization/cleanup exists; never erase source entries or relax acceptance to make import launchable.

Shared content planner/materializer and social/companion/inventory bridges can be implemented and fixture-tested independently, while actual full-floor wiring remains PENDING. Keep the original gameplay functions, catalog identity, inheritance, floor-wide caps, lifecycle/recall, container contents, lockpicking, cooldown/outcome history and input interactions in the separate original acceptance gates. Do not remove them because they are outside this leaf inventory.

Next bounded work: parent-owned corrected pool tests, new G5 metadata assertions and current-schema Editor staging; G1/G3 typed consumers; actual decal material closure and surface reservations before integration. Inspect every disposition/value, save only the exact new master through Editor APIs, repeat unchanged import for zero saves, and retain V6 unchanged. Separate native traversal/lighting receipts do not close full catalog, decal, migration or release gates.

## Audit checks and source fingerprints

The initial seven-family audit matched all 68 original pending paths exactly once: 25+3+6+10+5+13+6. G2/G6 changed only importer code, its pure tests and this report. The later G4 follow-up additionally changes the native Theme header and assertions in the two existing native tests. It performs no build, config, skill, asset or Editor operation. The following fingerprints describe the earlier audit, not current G4 source or compiled binaries.

| Inspected source path | SHA-256 |
| --- | --- |
| `Tools/Migration/Import-CalystoDungeonDirector58.py` | `a0d8b7019f844caa47568873e2f8fc11984ff9385c1407b0bc6f323faaa23489` |
| `Plugins/EFProcedural/Source/EFProceduralRuntime/Public/Calysto/EFCalystoDirectorTypes.h` | `83ae5a3c1909d2b232d754763b5bb2478b3d5692a813b31e51fcc96a5acdabfd` |
| `Plugins/EFProcedural/Source/EFProceduralRuntime/Private/Calysto/EFCalystoDungeonDirectorAsset.cpp` | `f5f14ef04e85d802432403d283994c0df155c0381a63e5771269a88a19d78a2c` |
| `Plugins/EFProcedural/Source/EFProceduralRuntime/Private/Calysto/EFCalystoLightingResolver.cpp` | `ce57ef61af50a16a3d96fda805e378702051d3255726d30e4b41dea6bb37d717` |
| `Plugins/EFProcedural/Source/EFProceduralPCGRuntime/Private/Calysto/EFCalystoFloorLightingComponent.cpp` | `49adb00f490aa78cfd9ff25b9af36fffa4f9788caa4e67adf0f04cae445a1467` |
| `Plugins/EFProcedural/Source/EFProceduralPCGRuntime/Private/Calysto/EFCalystoNativeAdapter.cpp` | `743fba3ba43c9a457331e40e1a3a39ab4c7a74eb21d1b021b9d4e89ef5fe7886` |
| `Plugins/EFProcedural/Source/EFProceduralRuntime/Private/Calysto/EFCalystoContentReservationPlanner.cpp` | `e315e5493113b82e2d448b53f0c73b11bfdc24cbe7e4821f480afc9285a5aa11` |

Audit UTC: `2026-09-08T09:55:41.108225+00:00`. Preserved worktree context: parent snapshot `Saved/Migration/CalystoDungeonDirectorV7/Resume_20260908/SourceBefore.zip`; this report grants no new acceptance PASS.

G7 source fingerprints before parent-owned compilation (the source snapshots above remain historical):

| Changed path (under project root) | SHA-256 |
| --- | --- |
| `Plugins/EFProcedural/Source/EFProceduralRuntime/Public/Calysto/EFCalystoDirectorTypes.h` | `232ba6399cf2511b6cc46e671ea2bee9ee81d7533bc7efc578419fbd89b83137` |
| `Plugins/EFProcedural/Source/EFProceduralRuntime/Private/Calysto/EFCalystoDungeonDirectorAsset.cpp` | `ee46b2c358aca2ef025ad55a8bfe7b5ea4c687bfb6ae366a3762ed31d3ad9768` |
| `Plugins/EFProcedural/Source/EFProceduralRuntime/Private/Calysto/EFCalystoContentReservationPlanner.cpp` | `bf59c9bcb88fbaf6dbd0fcd9200fa04cc9dfb5f50d7ccce02a7c45757e8e67c3` |
| `Plugins/EFProcedural/Source/EFProceduralRuntime/Private/Tests/EFCalystoDirectorFoundationTests.cpp` | `616105bd527a6305275d184f0f8058d5b22006e4aeb90243d7548c3f76ef8d61` |
| `Plugins/EFProcedural/Source/EFProceduralRuntime/Private/Tests/EFCalystoContentReservationPlannerTests.cpp` | `7deeaef07f1c4728cbbc97203c07e5e4483eb2ffcf244c1ed004e5f6c473d155` |
| `Tools/Migration/Import-CalystoDungeonDirector58.py` | `34d17c5bf026414726dee3fb6734d97ac7bad2a17027e462cdad7e834cb8feb5` |
| `Tools/Migration/Test-CalystoDirectorImporter58.py` | `6a3aee5fdc01104cb863f1d5bffa9b313943ab8fbb42788a5b714b33ec5fe75d` |

G2/G6 follow-up UTC: `2026-09-08T10:16:34.769598+00:00`. Test-source SHA-256: `98ff16115f16c0cabd816913d5a2263e726a4933e03c3f2bc5b67ffe64bbb584`; current importer SHA-256 is in the table. Original staged/source JSON artifacts were preserved.
