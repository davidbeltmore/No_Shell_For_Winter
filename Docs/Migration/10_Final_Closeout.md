# Final closeout

Status: `NOT_READY`

NoShellForWinter has a sanitized public documentation set and a canonical target architecture based on Inner Doctrine, DXP, Curse, Cursed, Guard Recovery, Charisma-gated social interactions, a neutral-primary single-game presentation, and protected Chronicle behavior. The current Intimacy implementation replaces Lust with independent temporary Player and Partner Climax values, repeatable orgasms that do not terminate the session, and one session-local Orgasm Rush state.

The final design has no separate SFW/NSFW selector and no gameplay-content opt-ins. At Charisma level 10, Intimacy is routed through immediate `T`+`Hyphen`/`Subtract` and `T`+`Y` `Partner > Intimacy` for a compatible Male companion or registered Male Enemy target. The normal authored companion route honors its social and allowed-location metadata; the targeted Charisma-10 adapter has an explicit companion/Enemy allowlist and overrides hostile classification for those targets. Verified-adult, bilateral-consent, life, consciousness, out-of-combat, and safe-cancellation rules remain authoritative; combat lockout and ACF controller/global-battle state are revalidated during preflight and every active tick. Intimacy does not consume `Space`, `Enter`, or arrow navigation while no session is active. Talk/minigame bonuses only accelerate a participant's Climax. While active, the session removes exactly one percent of Curse maximum per second; it grants no DXP, combat power, forced recruitment, or exclusive progression. Its dual Climax HUD follows the active HUD Theme. A real minigame defeat independently selects the retained mature vignette 10 percent of the time and directly respawns 90 percent of the time. Enemy barks use neutral substitutions 90 percent of the time and byte-exact protected originals 10 percent of the time. `-StreamerSafe` suppresses all adult presentation. The updated hostile-target path passed its scoped build, focused automation, PIE, cook/package, and packaged smoke gates; packaged Intimacy interaction, physical OS chord capture, every HUD Theme preset, Frederick runtime resolution, full-project Blueprint compilation, and broader release gates remain `PENDING`.

The final scoped Intimacy UE 5.8 Development Editor build (`Saved/Logs/IntimacyRework_EditorBuild_HostileTarget.log`), final 8/8 native automation suite (`Saved/Migration/Automation/IntimacyRework_HostileTarget_20260802_131200/index.json`), directed no-save compile of `BP_IntimacyScene_0001` and `WBP_ProjectEmoteMenu`, focused hostile-target PIE, representative active-theme menu/HUD visual QA, final Development Game compilation plus cook/stage/Pak/IoStore/archive, packaged launch/media smoke, and final no-new-delta invariant comparison are `PASS` within their named scopes. Evidence: [Intimacy Climax rework](Evidence/Intimacy_Climax_Rework_20260802.md). `Saved/Migration/Evidence/IntimacyRework_RuntimePIE_HostileTarget.json` proves a real `ACFMeleeEnemyBPMale` target can be selected at Charisma 10 despite hostility, enter `Actions.Together.0001Scene`, repeat Climax/orgasm, reduce Curse, and cancel safely; the companion regression remains in `IntimacyRework_RuntimePIE_Final4.json`. The visual receipts prove `Partner > Intimacy` and the active themed HUD. Input bindings are statically verified and driven through deterministic binding-equivalent harnesses; no physical operating-system capture of `T`+`Hyphen`/`Subtract` or `T`+`Y` is claimed. `Saved/Logs/IntimacyRework_UAT_HostileTarget.log` records `ProjectIntimacySubsystem` Game compilation and updated cook/stage/Pak/IoStore/archive with `BUILD SUCCESSFUL`, `ExitCode=0`. `Saved/Logs/IntimacyRework_PackagedSmoke_HostileTarget.log` proves engine initialization, Pak/IoStore mount, and exit 0, not an Intimacy interaction inside the package.

The canonical Development Editor build is `BLOCKED_EXTERNAL` before project code compilation by the protected installed DazToUnreal module `RulesError`; see `Saved/Logs/IntimacyRework_CanonicalBuild.log`. Development Game packaging does not clear that vendor blocker.

Closeout is blocked until the current implementation passes:

- resolution of the protected DazToUnreal vendor blocker and canonical UE 5.8 Development Editor/Game builds with the complete plugin set;
- compilation of every target-owned Blueprint; only the directed Intimacy scene/menu pair has passed;
- the remaining global PIE matrix in `08_QA_Matrix.md`; focused Intimacy functionality has passed, but physical OS key-chord capture remains unclaimed;
- visible QA for every required HUD Theme preset and the remaining single-game neutral presentation, mature-defeat routing, and Streamer Safe suppression; the active-theme Intimacy menu/HUD captures have passed;
- targeted project-owned asset resave followed by removal of every temporary redirect;
- packaged Intimacy interaction, including partner selection, entry, HUD, repeated Climax, Curse, cancellation, and Streamer Safe behavior; updated package bootstrap under `Saved/Migration/Package/IntimacyRework_HostileTarget_20260802/{Stage,Archive}` and launch/media smoke have passed;
- Frederick authoritative runtime path/hash resolution; `Saved/Migration/Evidence/IntimacyRework_Final_ProtectedInvariants.json` confirms the final ACFU, DazToUnreal, Player, Female, Multiple, and Male comparison has 0 new deltas;
- final prohibited-identifier scan across source, config, docs, metadata, Asset Registry and packaged binaries.

Chronicle barks must retain SHA-256 `E76C4C4A50BB3DD98DB769BF820DF10BF81FFC6FB341E96FEB7B7B25DBF6185A`.

No unexecuted gate may be described as `PASS`. Current blockers are tracked in `09_Known_Issues.md`.
